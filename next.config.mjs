/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static export removed — site now server-rendered on Vercel (Phase 13+)
  // Required for: admin UI, DB-backed content, live discovery pipeline
  // To deploy: push to GitHub → connect Vercel → set env vars from .env.example
}

export default nextConfig
